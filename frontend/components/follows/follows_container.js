import { connect } from 'react-redux'
import follows from './follow'

const mSTP = (state) => {
  return {
    
  }
}

const mDTP = dispatch => ({
  
})

export default connect(mSTP, mDTP)(follows);